create database empresa;
use empresa;

create table cliente(
codigo varchar(100),
nome varchar(100),
endereco varchar(100),
cpf varchar(100),
telefone varchar(100)
);

create table compra(
codigo varchar(100),
quantidade varchar(100)
);

create table funcionario(
codigo varchar(100),
nome varchar(100),
cpf varchar(100),
telefone varchar(100)
);

create table departamento(
codigo varchar(100),
descrição varchar(100)
);

create table fornecedor(
codigo varchar(100),
nome varchar(100),
telefone varchar(100)
);

create table produto(
codigo varchar(100),
descrição varchar(100),
valor varchar(100)
);



describe cliente;
describe compra;
describe funcionario;
describe departamento;
describe fornecedor;
describe produto;


select * from cliente;
select * from compra;
select * from funcionario;
select * from departamento;
select * from fornecedor;
select * from produto;



insert into cliente(codigo, nome, endereco, cpf, telefone) 
values(12, 'tiago', 'rua tal', 08475847533, 08987435 )
insert into compra(codigo, quantidade) 
values(12, 20)
insert into funcionario(codigo, cpf, telefone) 
values(19, 85748574833, 96875904)
insert into departamento(codigo, descricao) 
values(190, 'sla')
insert into fornecedor(codigo, nome, telefone) 
values(12,'tiago', 958495789)
insert into produto(codigo, valor)
values(18, 20)
